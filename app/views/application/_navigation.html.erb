<div class="container py-2 fixed-top">
  <nav class="navbar navbar-expand-lg navbar-dark bg-dark">
    <h1 class="navbar-brand">
      Jeanny &amp; Tataihono
    </h1>
    <% if invite&.persisted? %>
      <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
      </button>
      <div class="collapse navbar-collapse" id="navbarSupportedContent">
        <ul class="navbar-nav ml-auto">
          <li class="nav-item <%= 'active' if current_page?(root_path) %>">
            <%= link_to 'Home', root_path, class: 'nav-link' %>
          </li>
          <li class="nav-item <%= 'active' if current_page?(edit_invite_path) %>">
            <%= link_to 'RSVP', edit_invite_path, class: 'nav-link' %>
          </li>
          <li class="nav-item <%= 'active' if current_page?(what_is_marriage_path) %>">
            <%= link_to 'What is Marriage?', what_is_marriage_path, class: 'nav-link' %>
          </li>
          <li class="nav-item">
            <%= link_to 'Sign Out', sign_out_path, method: :delete, class: 'nav-link' %>
          </li>
        </ul>
      </div>
    <% else %>
    <ul class="navbar-nav ml-auto">
      <li class="nav-item active">
        <a class="nav-link" href="<%= root_path %>"><i class="fal fa-church"></i></a>
      </li>
    </ul>
    <% end %>
  </nav>
</div>
